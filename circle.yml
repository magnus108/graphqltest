machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t magnus108/graphqltest:$CIRCLE_SHA1 .

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh
